spring:
  application:
    name: auth-server

server:
  port: 8070

#logging.level.org.springframework.security: DEBUG

#logging.leve.org.springframework: DEBUG

security:
  oauth2:
    resource:
      filter-order: 3
